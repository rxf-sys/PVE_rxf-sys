# Audit-Auswertung 2026-06-12 (Live-Daten)

> Auswertung der Infosammlung vom 2026-06-12 09:21 (collect-audit-info.sh).
> Ergänzt [PLAN-best-practice-2026-06.md](PLAN-best-practice-2026-06.md) um Ist-Werte.

## Gesamtbild

System ist gesund: keine failed units, Storage 19 % belegt, beide SSDs SMART-PASSED
(NVMe 7 % wear nach 13 Monaten, sda quasi neuwertig), Zeitsync sauber (Offset µs),
Microcode aktuell, Security-Repo aktiv, fail2ban läuft, Firewall enabled/running.

## Bereits erledigt (besser als vermutet)

| Vermutung | Status |
|-----------|--------|
| intel-microcode fehlt | ✅ installiert (rev 0xfa, SRBDS mitigiert) |
| CT-Swap auf Default 512 MB | ✅ überall bereits `swap: 0` |
| Docker fällt auf vfs zurück | ✅ alle Docker-CTs nutzen overlayfs |
| chrony/Zeitsync prüfen | ✅ aktiv, Stratum 3, Offset < 1 ms |
| smartd fehlt | ⚠️ teilweise: läuft mit `-m root`, aber **keine Self-Test-Schedule** und root-Mail vermutlich Sackgasse (s. F2) |
| Journald nicht persistent | ✅ persistent, nur 68 MB |
| IPv6 ungeklärt | ✅ kein globales IPv6 (nur link-local) → kein Handlungsbedarf |

## Neue Funde (nach Priorität)

### F1 — Reboot ausstehend 🔴
Kernel 7.0.6-2 ist installiert, es läuft aber noch 7.0.2-7 (Uptime 13 Tage),
plus 3 offene Paket-Updates. → Beim nächsten Wartungsfenster: Updates + Reboot.

### F2 — root-Mail vermutlich Sackgasse 🔴
smartd alarmiert mit `-m root`, aber: kein root-Alias in `/etc/aliases` und
`proxmox-mail-forward` wurde nicht im PATH gefunden. Wenn die Kette
root-Mail → Notification-System unterbrochen ist, verpuffen smartd-/Cron-Alarme.
→ Testmail schicken, ggf. `/root/.forward` + Paket fixen (Snippet unten).

### F3 — 250 Unsafe Shutdowns auf der NVMe 🟠
Bestätigt die USV-Empfehlung (Plan 2.7) mit hartem Messwert. Jeder davon ist
ein Risiko für LVM-Thin-Metadaten und den PBS-Datastore.

### F4 — CT 101 + CT 109 Disks laufen voll 🟠
- CT 101: Dateisystem 80 % (12/16 GB), Thin-LV 89,9 %
- CT 109: Dateisystem 71 % (11/16 GB), Thin-LV **91,3 %**

Die Differenz FS↔Thin-LV (109: 71 % vs 91 %) heißt: gelöschte Blöcke wurden nie
per Discard an den Thin-Pool zurückgegeben — der Host-`fstrim.timer` trimmt
CT-Dateisysteme NICHT. → Docker-Aufräumen + `pct fstrim` für alle CTs als Timer.

### F5 — Prune läuft NACH GC (bestätigt)
GC 03:30, Prune 04:00, kein `verify-new` am Datastore, **kein Sync-Job (Off-Site
weiterhin offen)**. Verify-Job läuft sonntags (Doku sagt samstags — Drift).

### F6 — vzdump-Encryption-Key FEHLT (bestätigt)
`/etc/pve/priv/storage/pbs.enc` existiert nicht → Plan 1.4 weiter offen,
Voraussetzung für Off-Site.

### F7 — Kein protection-Flag, Startup-Order unvollständig (bestätigt)
Kein einziger Gast hat `protection: 1`. Startup-Order nur bei VM 200 (3) und
VM 201 (1) — CT 104 (Tunnel) und CT 100 (NAS) starten unkontrolliert.

### F8 — Features inkonsistent (bestätigt, präzisiert)
- Docker-CTs **101 und 109 haben kein `keyctl=1`** (102/103/104 schon)
- Non-Docker-CTs **100, 106, 107, 108 haben unnötig `nesting=1`**

### F9 — Timezone in allen CTs leer
Alle 9 CTs laufen auf UTC (kein /etc/timezone) — Host ist CEST. Log-Korrelation
Host↔CT ist um 2 h versetzt. → `pct set <id> --timezone host`.

### F10 — Kleinkram
- `vm.swappiness` = 60 (Empfehlung: 10)
- `pve-ha-lrm`/`pve-ha-crm` aktiv ohne Cluster → abschalten (SSD-Schreiblast)
- `datacenter.cfg` ohne `email_from`
- Governor `performance` → `powersave` spart beim i5-9500T ein paar Watt (optional)
- SSH: 2 Keys vorhanden → Umstieg auf key-only (Plan 2.4) ist jetzt gefahrlos
- KSM teilt 0 Pages (RAM-Druck zu niedrig) — ok, kein Handlungsbedarf

### F11 — Gute Nachricht fürs Storage-Backup (Plan 1.2)
`/mnt/storage` belegt nur **145 GB**, PBS-Datastore hat **391 GB frei** →
ALLES inkl. `media/` passt nach PBS (dedupliziert sogar deutlich kleiner).
Die Ausnahme-Überlegung für media/ entfällt.

### F12 — Doku-Drift (Repo aktualisieren)
- CT 109: real 4 GB RAM / 16 GB Disk (Doku: 2 GB / 8 GB)
- CT 101: Tags real `monitoring;website` (Doku: `website`)
- Immich-Stack: real `ghcr.io/immich-app/postgres:14-vectorchord` + `valkey/valkey:8`
  (Doku: `tensorchord/pgvecto-rs` + `redis:alpine`)
- PBS-Verify: real `sun 05:00` (Doku: Sa 05:00)
- CT 102 Tag `vault`, CT 100 nutzt `nameserver`/`searchdomain` — in Doku ergänzen

## Plan-Abgleich (offene Phase-1/2-Punkte unverändert gültig)

Off-Site-Sync (1.1), /mnt/storage-Backup (1.2), Cloudflare Access (1.3),
2FA + Encryption (1.4), OnFailure-Mails (2.1), USV (2.7), externer
Monitoring-Check (2.8) — alle durch Live-Daten bestätigt, keiner erledigt.
